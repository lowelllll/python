x (변수)= 10(값,객체) 
10의 refcount(참조횟수)++

바이트 문자열 : 인코딩되어있는것으로 간주
유니코드 문자열: 이노딩이 되어있지 않은 무가공 문자값들을 표현 
바이트 문자열과 유니코드문자열을 절대 혼하배서 사용 x

format
a = "%10(변경자)d(변환문자)"(변환지정자) %6

str() 문자열
repr() 표현식 문자열


a and b 일때  a가 거짓이라면 b를 평가하는 코드는 실행되지 않는다.
(단축평가) 
플래그 변수: 동작을 수행할지 말지 정하는 변수
ex) flag = false
	if flag:
		pass

with : 제어흐름이 블록을 벗어날때 열린파일을 자동으로 닫아줌
	블록에 진입하고 ㅃ져나올때 일어나는 일을 객체 obj 에서 할 
수 있게 해줌
